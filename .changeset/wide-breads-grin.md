---
'@jagreehal/workflow': minor
---

Add documentation for composing workflows together. Workflows can be combined by calling one workflow from within another using `step()`, and error types automatically aggregate into a union.
